# 转录结果目录说明

VoiceScript 默认把转录结果写入项目目录下的 `transcripts` 文件夹。用户也可以在桌面端右侧设置中选择自定义保存位置。

## 文件格式

- `.txt`：纯文本时间分段转录。
- `.md`：Markdown 表格形式，适合放入 Obsidian。
- `.srt`：字幕格式。
- `.json`：结构化数据，保留源文件、模型、语言、总时长和 segments。

## 内容边界

所有导出都只包含原始转录文字和时间戳。不包含总结、会议纪要、行动项、语义标签或说话人分离。
